# Patient-facing explanation layer (Stage 4).
# Deterministic, template-based only — no LLM generation anywhere in this package.
# NOT yet wired into main.py / the production pipeline (pending approval).
